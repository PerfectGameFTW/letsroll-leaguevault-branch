export const searchStorageKey = (locationId: number) =>
  `league-square-catalog:search:${locationId}`;

export const categoryStorageKey = (locationId: number) =>
  `league-square-catalog:category:${locationId}`;
